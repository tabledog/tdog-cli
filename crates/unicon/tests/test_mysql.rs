// @todo/next

// - update/insert
//      - replace update with last_write_ts?
// - JSON and DateTime cols.
// - Deletions (where works).

// FromValue - test all types of Value (read from database, instantiate struct).

// Test u64 insertion into an INT col (i32?), is an error thrown?
// - Add BIGINT type for MySQL?